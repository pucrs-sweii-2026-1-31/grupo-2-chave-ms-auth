meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: none
}

body:json {
  {
    "email": "john@example.com",
    "password": "secret123"
  }
}

vars:post-response {
  accessToken: res.body.access_token
  refreshToken: res.body.refresh_token
}
