meta {
  name: List Users
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/users?limit=10&offset=0
  body: none
  auth: bearer
}

params:query {
  limit: 10
  offset: 0
}

auth:bearer {
  token: {{accessToken}}
}
